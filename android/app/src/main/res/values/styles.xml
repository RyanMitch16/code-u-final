<resources>

    <style name="LoginScreen" parent="Theme.AppCompat.NoActionBar">
    </style>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/material_primary</item>
        <item name="colorPrimaryDark">@color/material_primary_dark</item>
        <item name="android:listViewStyle">@style/ListTheme</item>
    </style>


    <style name="ListTheme">
        <item name="android:textColor">@color/material_text_primary</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:background">@color/material_list_background</item>
        <item name="android:divider">@color/material_divider</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingLeft">5dp</item>
    </style>


</resources>
